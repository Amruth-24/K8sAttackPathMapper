# GuardV2: Kubernetes Attack Path Visualizer

GuardV2 is an automated threat modeling tool that identifies critical attack paths and privilege escalation risks within a Kubernetes cluster.

## 🚀 Quick Start with Docker

### 1. Build the Image
```bash
docker build -t guardv2 .